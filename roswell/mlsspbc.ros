#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload 'mlsspbc :silent t)
  )

(defpackage :ros.script.mlsspbc
  (:use :cl :mlsspbc))
(in-package :ros.script.mlsspbc)

(defun main (command &rest argv)
  (declare (ignorable command argv))
;;  (cond (t;;(string= command "start-system")
  (mlsspbc::start-system))
	;; ((string= command "stop-system")
	;;  (mlsspbc::stop-system))))
;;; vim: set ft=lisp lisp:
